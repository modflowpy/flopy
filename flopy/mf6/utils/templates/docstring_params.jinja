{%- for v in variables.values() recursive %}
    * {{ v.name }} : {{ v._type }}
{%- if v.description is defined and not v.is_choice %}
{{ v.description|wordwrap|indent(4 + (loop.depth * 4), first=True) }}
{%- endif %}
{%- if v.children is defined and loop.depth < 2-%}
{{ loop(v.children.values())|indent(4) }}
{%- endif %}
{% endfor -%}